class Solution {
    public boolean isPalindrome(int x) {
        if(x<0) return false;
        int rev=0;
        int rem;
        int num=x;
        while(num!=0){
            rem=num%10;
            num=num/10;
            rev=rev*10+rem;
        }
        return (x==rev);
    }
}
